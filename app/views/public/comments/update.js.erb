console.log("update.js.erb loaded for comment:", "<%= @comment.id %>");

const commentId = "<%= @comment.id %>";
const commentBody = document.getElementById(`comment-body-${commentId}`);
const editForm = document.getElementById(`edit-comment-${commentId}`);

if (commentBody) {
  commentBody.innerText = "<%= j @comment.body %>";
} else {
  console.warn(`comment-body-${commentId} が見つかりませんでした`);
}

if (editForm) {
  editForm.remove();
} else {
  console.warn(`edit-comment-${commentId} が見つかりませんでした`);
