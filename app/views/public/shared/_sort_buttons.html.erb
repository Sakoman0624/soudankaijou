<div class="mb-3">
  <div class="btn-group" role="group" aria-label="ソート順">
    <%= link_to 'いいね多い順', url_for(request.query_parameters.merge(sort: 'likes_desc')), class: "btn #{params[:sort] == 'likes_desc' ? 'btn-primary' : 'btn-outline-primary'}" %>
    <%= link_to '更新が新しい順', url_for(request.query_parameters.merge(sort: 'updated_desc')), class: "btn #{params[:sort] == 'updated_desc' ? 'btn-primary' : 'btn-outline-primary'}" %>
    <%= link_to '更新が遅い順', url_for(request.query_parameters.merge(sort: 'updated_asc')), class: "btn #{params[:sort] == 'updated_asc' ? 'btn-primary' : 'btn-outline-primary'}" %>
    <%= link_to '作成が新しい順', url_for(request.query_parameters.merge(sort: 'created_desc')), class: "btn #{params[:sort] == 'created_desc' ? 'btn-primary' : 'btn-outline-primary'}" %>
    <%= link_to '作成が古い順', url_for(request.query_parameters.merge(sort: 'created_asc')), class: "btn #{params[:sort] == 'created_asc' ? 'btn-primary' : 'btn-outline-primary'}" %>
  </div>
</div>
